第一类：数据清洗与预处理 (20题)
测试点：模糊意图识别、异常值处理、文本清洗

把表格里所有的空单元格都填上“未知”。
帮我删掉那些没有联系方式的行。
纠正一下 A 列里所有的日期格式，统一成 YYYY-MM-DD。
客户姓名那一列好像有些前后的空格，帮我去掉。
把所有“金额”列里的货币符号去掉，只保留数字。
检查一下有没有重复的订单号，如果有，只保留最新的那条。
把所有的“男”改成“M”，“女”改成“F”。
我看“备注”列太乱了，把里面的 HTML 标签都删干净。
把那一列看起来像手机号但位数不对的都标红。
这一列的文本全部转成大写。
这里的百分数怎么是小数？帮我转成百分比格式，保留两位小数。
删掉所有销售额为 0 或者负数的记录。
把地址分列，省份单独一列，城市单独一列。
把 B 列里的所有“N/A”都替换成 0。
去掉表格里所有的空行。
把“产品描述”里所有的换行符都去掉。
姓名那一列，把姓和名分来，放在两列里。
这一列不仅有数字还有单位，帮我把单位“kg”去掉，只要数字。
把所有全角字符转成半角。
帮我修补一下邮编，如果少于 6 位，前面补 0。

第二类：多表关联与合并 (15题)
测试点：DeepSeek 的逻辑推理、Schema 理解、VLOOKUP 替代

帮我根据“员工ID”，把表 2 里的“部门名称”匹配到表 1 里来。
给我对比一下这两个月的销售表，找出哪些客户上个月买了但这个月没买。
把这三个地区的分表合并成一张总表。
用“产品型号”做关联，算出每个产品的总库存成本（库存表 x 价格表）。
查找表 B 里没有在表 A 中出现过的所有 SKU。
把 2024 年的数据和 2025 年的数据拼在一起，加一列标记年份。
根据身份证号，从另一个表里把出生日期填过来。
这两个表都有“销售额”，帮我合并后算个总数。
表 1 是花名册，表 2 是打卡记录，帮我找出今天没打卡的人。
把所有分公司的表格汇总，按“季度”求和。
匹配一下，只要是 VIP 客户名单里的订单，都在主表里标个星。
两个表的列名不一样，一个是“Tel”，一个是“电话”，能根据内容合并吗？
找出两个表格里完全重复的行，然后删掉。
左边这个表是主表，把右边表里的相关信息填进去，匹配不上的留空。
把这 12 个月的月度报表合并，并按时间排序。

第三类：复杂筛选与查询 (15题)
测试点：多条件逻辑、自然语言理解

帮我挑出所有广东地区的、且消费超过 5000 元的男性客户。
只要看最近一周的数据，其他的都隐藏。
筛选出那些备注里包含“投诉”或者“退款”的记录。
找出销售额最高的前 10 名销售员。
帮我看看哪些产品的库存周转天数超过了 100 天。
排除掉所有状态是“已取消”或者“待审核”的订单。
只要周六和周日的记录。
筛选出名字是三个字的所有人。
找出那些单价高于平均值的产品。
只要看 2025 年第一季度的所有电子产品销售记录。
筛选出“数量”大于 10 且“折扣”小于 0.9 的行。
把所有姓“张”或者姓“李”的客户找出来。
只要看最后 50 行数据。
找出这一列里数值最大的 5% 的数据。
筛选出所有在这个时间段（9:00 - 18:00）之外的登录记录。

第四类：统计分析与计算 (20题)
测试点：代码沙箱能力、聚合计算

算一下每个部门的平均工资是多少。
帮我统计一下各个省份的客户数量，按数量降序排列。
算一下这个月的总销售额，再算一下环比增长了多少。
给我做一个透视表，看不同产品在不同地区的销量。
这一列的数字太长了，帮我求个和，然后告诉我结果。
计算每一行的“单价”乘以“数量”，把结果放在新的一列叫“总价”。
统计一下男女比例是多少。
帮我把这一列的分数按 10 分一个档次进行分段统计。
算出每个人的绩效得分，规则是：销售额 x 0.8 + 满意度 x 0.2。
帮我看看哪个月的销售波动最大（标准差）。
统计每个类别的退货率（退货数 / 总订单数）。
算一下累计销售额，就是每一行都要加上前面的总和。
找出销售额贡献度最高的 20% 的客户（帕累托分析）。
计算这一列数据的中位数和众数。
帮我预测一下下个月的销量大概是多少（线性回归）。
统计每个销售员完成了多少单，并算出完成率（目标在另一列）。
把所有负数都转成绝对值后再求和。
算一下两列数据的相关系数，看它们有没有关系。
按周统计销售总额。
只要“完成”状态的订单金额总和。

第五类：格式美化与可视化 (15题)
测试点：原子指令调用、UI 反馈

把所有不及格的分数（小于 60）标成红色。
给表头加个深蓝色的背景，字体变白加粗。
把销量最高的前三名背景标成金色。
帮我把所有列的宽度自动调整一下，适应内容。
给这一列加个数据条，一眼能看出大小。
把重复的值高亮显示出来。
将日期列的显示格式改成“2025年1月1日”。
隔行换色，让表格看起来清楚点。
帮我画个柱状图，看各部门的业绩。
生成一个饼图，展示各渠道的占比。
把超过平均值的单元格字体加粗。
冻结第一行，我往下拉的时候要能看到表头。
把所有的边框都加上，用细黑线。
根据状态列，如果是“完成”就标绿，是“失败”就标红。
生成一个折线图，看最近一年的趋势。

第六类：口语化与模糊指令 (10题)
测试点：意图推理、容错能力

那个...帮我把表弄得好看点。
只要红色的那些行，其他的都删了。
看看哪里有问题，帮我修修。
把最后面的那几列不要的都去掉。
这个表里的钱都换算成美元，大概按 7.2 算吧。
把那个叫什么“ID”的列挪到最前面去。
整理一下这个表。
看看谁卖得最好？
把乱码都清掉。
帮我算算我赚了多少。

第七类：高阶逻辑与生成 (5题)
测试点：AI 生成能力

根据现有的邮箱列，帮我生成一列假的手机号用于测试。
帮我把这一列的英文翻译成中文。
根据客户的购买记录，给每个客户打一个标签（如：高价值、沉睡用户）。
分析一下这个表的销售数据，给我写一段简短的总结报告。
这一列是评论，帮我分析一下用户的情感是正面还是负面。
